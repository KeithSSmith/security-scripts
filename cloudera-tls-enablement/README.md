# Cloudera Manager TLS Enablement

The purpose of this project is to help automate the process of TLS enablement for Cloudera cluster services.  This utilized the Cloudera Manager API to expedite the enablement process once certificates are available on each node in the cluster.

* Communication to the Cloudera Manager Server with proper Authorization

```vim
source enable-tls.sh
```
